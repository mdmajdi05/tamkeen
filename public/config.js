// Runtime configuration — edit this file in public_html WITHOUT rebuilding.
// Set API_URL to your backend base URL to switch from local JSON to live API.
window.__RUNTIME_CONFIG__ = {
  API_URL: "" // e.g. "https://api.tamkeen.com" — leave empty for local JSON fallback
};
